# ملاحظات مناقشة مشروع UnpackHelper

## الهدف

فهم المشروع خطوة خطوة كما في مناقشة أكاديمية، ثم إعادة هيكلته وتحسينه بناءً على ما نتوصل إليه.

## ما تم فهمه حتى الآن

### فكرة المشروع

المشروع يفحص الملفات التنفيذية ويحلل البصمات التي قد تدل على استخدام برنامج تغليف أو ضغط للملف، مثل UPX.

### المشكلة

من الصعب أحيانًا معرفة ما إذا كان الملف التنفيذي مغلفًا، وما نوع برنامج التغليف المستخدم فيه. التغليف قد يغيّر شكل الملف ويجعل تحليله أصعب.

### الفائدة

معرفة نوع الـ packer تساعد المحلل على فهم سبب اختلاف بنية الملف، واختيار خطوات وأدوات التحليل المناسبة، وفرز الملفات التي تحتاج إلى تحليل أعمق. اكتشاف packer لا يثبت أن الملف خبيث.

### UPX

UPX هو Ultimate Packer for eXecutables، ويستخدم لضغط الملفات التنفيذية مثل EXE وDLL لتقليل حجمها. من بصماته الشائعة `UPX!` وأسماء الأقسام `UPX0` و`UPX1` و`UPX2`.

### حدود المشروع

الأداة لا تفك التغليف ولا تشغّل الملف، بل تقوم بتحليل ساكن. النتيجة احتمالية وليست يقينًا بنسبة 100%، لأن البصمات يمكن تغييرها وقد تظهر نتائج خاطئة.

### نظام الثقة الحالي

تعتمد الأداة على جمع نقاط من عدة مؤشرات. في النسخة الحالية، توقيع `UPX!` يعطي 80 نقطة، و`UPX0` و`UPX1` يعطي كل منهما 15 نقطة. الدرجة 80 أو أكثر تعرض مستوى High.

### ملاحظة تحسين مهمة

ينبغي عند إعادة الهيكلة مراجعة نظام الثقة حتى لا يكون وجود توقيع واحد فقط كافيًا دائمًا لنتيجة High، بل تكون الثقة الأقوى عند اجتماع التوقيع مع أسماء الأقسام ومؤشرات أخرى.

## المرحلة الحالية في المناقشة

السؤال التالي هو: ما هو ملف PE؟

### EXE وDLL

ملف EXE هو برنامج يمكن تشغيله مباشرة، أما DLL فهي مكتبة ديناميكية تحتوي وظائف أو أكوادًا تستخدمها برامج أخرى. كلاهما قد يستخدم بنية PE.

### نقطة تحتاج إلى شرح

لم يعرف المستخدم بعد معنى أن يكون EXE وDLL من نوع PE، وسيتم شرح أن PE هي البنية/التنسيق الداخلي الذي تستخدمه Windows للملفات التنفيذية والمكتبات، وليس امتدادًا إضافيًا للملف.

## متطلبات إعادة البناء المعتمدة

- فحص ملف واحد بتقرير نصي افتراضي، مع JSON وHTML اختياريين.
- فحص مجلد كامل وجميع المجلدات الفرعية افتراضيًا، مع خيار `--no-recursive`.
- فحص كل الملفات، وليس الامتدادات فقط.
- تصنيف العناصر إلى `PE` و`Non-PE` و`Error` و`Skipped`.
- عدم اتباع الروابط الرمزية افتراضيًا، وتجاوز الملفات التي تتخطى حد الحجم مع تسجيل السبب.
- استمرار الفحص عند وجود ملف تالف أو غير قابل للقراءة.
- تقرير المجلد يعرض تفاصيل ملفات PE فقط، ويعرض Non-PE كعداد مختصر، مع أقسام للأخطاء والتجاوزات وملفات المراجعة.
- تجميع الملفات المتطابقة حسب SHA-256.
- أمر `compare` يقارن SHA-256 فقط بين ملفين.
- نظام تقييم `OK` و`REVIEW_REQUIRED` و`ANALYSIS_ERROR` و`SKIPPED`، دون اعتباره حكمًا أمنيًا قطعيًا.
- يوضع ملف PE للمراجعة عند وجود packer معروف، أو Entropy مرتفعة جدًا، أو قسم قابل للتنفيذ والكتابة، أو قسم مخصص عالي العشوائية.

## قاعدة التوقيعات الموسعة

تتضمن القاعدة UPX وMPRESS وASPack وPECompact وThemida/WinLicense وVMProtect وFSG وEnigma Protector وNsPack وPetite وPEBundle وPELock وRLPack وNeolite وUPack وWWPack وYoda Protector. العلامات النصية وأسماء الأقسام منفصلة، والمؤشر المنفرد لا يثبت packer بشكل قطعي.

تمت إضافة `signature_research.md` لتسجيل المصادر وحدود البصمات. آخر تشغيل بعد التوسعة ومحرك التقييم نجح في 11 اختبارًا.
