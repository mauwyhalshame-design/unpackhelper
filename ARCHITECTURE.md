# معمارية مشروع UnpackHelper

## الهدف

UnpackHelper أداة تحليل ساكن لملفات Windows PE تبحث عن مؤشرات تدل على استخدام برامج تغليف أو ضغط تنفيذية. لا تقوم الأداة بتشغيل الملف المفحوص.

## مسار التنفيذ

```text
main.py
  ↓
cli.py
  ↓
config.py
  ↓
pe_parser.py
  ↓
fingerprint.py
  ↓
report.py
  ↓
entropy.py / html_report.py
```

## مسؤولية كل ملف

| الملف | المسؤولية | المدخلات | المخرجات |
|---|---|---|---|
| `main.py` | تنسيق سير العمل ومعالجة أخطاء CLI | أوامر المستخدم | Exit code وتقرير |
| `cli.py` | تعريف الأوامر والخيارات | نص الأمر | كائن arguments |
| `config.py` | قراءة والتحقق من JSON | مسار config | إعدادات صالحة |
| `errors.py` | تعريف الأخطاء المتوقعة | لا ينفذ تحليلًا | `AnalysisError` |
| `pe_parser.py` | قراءة بنية PE دون تشغيل الملف | مسار الملف | `PEInfo` |
| `signatures.py` | قاعدة معرفة البصمات | قواعد ثابتة | `SIGNATURES` |
| `fingerprint.py` | تطبيق القواعد وحساب الدرجات | `PEInfo` | detections وأدلة |
| `report.py` | بناء التقرير وعرضه | معلومات الكشف | نص أو JSON |
| `entropy.py` | حساب Shannon Entropy للأقسام | bytes القسم | قيمة 0–8 وتصنيف وصفي |
| `html_report.py` | إنشاء تقرير HTML مستقل | نتيجة التحليل | ملف HTML قابل للفتح محليًا |

## دورة تحليل ملف

أولًا يتحقق البرنامج من وجود الملف وإمكانية قراءته وحجمه. بعد ذلك يقرأ البيانات الثنائية ويتحقق من `MZ` وتوقيع `PE\0\0`. ثم يقرأ عدد الأقسام وأسماءها وبعض معلومات الترويسة. بعد ذلك تطبق وحدة `fingerprint.py` قواعد البصمات مثل `UPX!` و`UPX0`. أخيرًا تجمع `report.py` الهوية الرقمية، نوع الملف، الأدلة، الدرجات، ومستوى الثقة في تقرير قابل للقراءة.

## مبدأ الثقة

لا يمثل اسم packer حقيقة قطعية. marker المنفرد دليل محدود، بينما اجتماع marker واسم قسم مناسب يعطي دليلًا أقوى. لذلك تعرض الأداة `High` أو `Medium` أو `Low` بدل نتيجة ثنائية فقط.

## قرارات السلامة

يتعامل البرنامج مع العينة كبيانات ثنائية للقراءة فقط. لا يستدعي الملف ولا ينفذه ولا يستخدم أدوات خارجية لفك التغليف. اكتشاف packer لا يعني أن الملف ضار، وعدم اكتشافه لا يعني أن الملف آمن.

## ملاحظات التشغيل

التشغيل المقصود من داخل مجلد المشروع مباشرة:

```bash
python main.py scan sample.exe
```

في Linux يستخدم غالبًا `python3`، وفي Windows يمكن استخدام `py` أو `python`. يعتمد المشروع على المكتبة القياسية فقط، ولا توجد حزم خارجية في `requirements.txt`.
