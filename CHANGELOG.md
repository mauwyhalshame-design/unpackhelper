# سجل تغييرات مشروع UnpackHelper

هذا الملف يسجل ما تم عمله في المشروع وسبب كل قرار ونتيجة التحقق منه.

## 2026-09-05 — توثيق ومراجعة النسخة الحالية

### القرار
اعتماد توثيق مستمر لكل تعديل بدل إجراء تغييرات غير مسجلة.

### السبب
حتى يمكن شرح المشروع في التقرير والمناقشة، ومعرفة سبب كل تعديل ونتيجة كل اختبار.

## 2026-09-05 — إصلاح اختبار إعدادات JSON

### المشكلة
كان اختبار `config_validation` موجودًا بعد كتلة التشغيل المباشر في ملف الاختبارات، لذلك لم يكن `unittest discover` يكتشفه.

### الإصلاح
نُقل الاختبار إلى داخل الصنف `TestProject` وقبل:

```python
if __name__ == "__main__":
    unittest.main()
```

### النتيجة
ارتفع عدد الاختبارات المنفذة فعليًا من 4 إلى 5، ونجحت جميعها.

## 2026-09-05 — تحسين نظام الثقة

### المشكلة
كان توقيع واحد مثل `UPX!` قادرًا على إعطاء درجة مرتفعة دون وجود أدلة مساندة.

### الإصلاح
تم تعديل الحساب بحيث تكون الثقة `High` عند اجتماع marker مع اسم قسم مناسب ودرجة كافية. أما marker المنفرد فيصنف غالبًا `Medium`.

### النتيجة
أصبحت النتيجة أكثر تحفظًا وقابلية للتفسير، مع تقليل احتمال النتائج الخاطئة.

## 2026-09-05 — تحسين التقرير

### التعديل
أضيف نوع الدليل إلى التقرير، مثل `marker` أو `section`، وأصبح بالإمكان حفظ تقرير JSON في ملف باستخدام `--output` مع `--json`.

### مثال

```bash
python main.py scan sample.exe --json --output report.json
```

## 2026-09-05 — تحسين الإعدادات

### التعديل
أصبح `config.example.json` يتحكم فعليًا في `max_file_size`، وليس مجرد ملف يتم التحقق من صيغته.

## 2026-09-05 — توافق الأنظمة

### التحقق
تم تشغيل الاختبارات وأوامر CLI في بيئة Linux. يعتمد المشروع على Python القياسية و`pathlib`، وتوجد أوامر موثقة لـ Windows PowerShell وCMD وLinux.

### ملاحظة
يجب إجراء تشغيل يدوي نهائي على Windows قبل العرض للتأكد من البيئة الفعلية.

## 2026-09-05 — الاختبارات النهائية الحالية

نجحت الاختبارات الخمسة التالية:

| الاختبار | النتيجة |
|---|---|
| إعدادات JSON صحيحة وغير صحيحة | ناجح |
| marker منفرد لا يعطي High تلقائيًا | ناجح |
| اكتشاف UPX مع عدة بصمات | ناجح |
| رفض ملف غير PE | ناجح |
| رفض ملف غير موجود | ناجح |

## القيود المعروفة

لا تدعي الأداة اكتشاف كل packers. بعض البصمات يمكن تغييرها، والبحث النصي وحده قد يسبب نتيجة خاطئة. لذلك تعرض الأداة الأدلة ودرجة الثقة، وتعتبر النتيجة مساعدة تحليلية لا حكمًا قطعيًا.

## 2026-09-05 — إضافة التحليل الذكي وتقرير HTML

### الميزة
أضيفت وحدة `entropy.py` لحساب Shannon Entropy لكل قسم، مع تصنيف وصفي `Low` و`Moderate` و`High` و`Very high`.

### الهدف
تقديم دليل إضافي قابل للتفسير حول طبيعة بيانات القسم. القيمة العالية قد تتوافق مع ضغط أو تشفير، لكنها ليست دليلًا على الخبث.

### التقرير
أضيفت وحدة `html_report.py` وخيار CLI:

```bash
python main.py scan sample.exe --html report.html
```

يعرض التقرير HTML معلومات الملف، SHA-256، packer المحتمل، الثقة، الأدلة، وخصائص الأقسام.

### التحقق
أضيفت اختبارات Entropy وHTML، وأصبحت مجموعة الاختبارات تحتوي على 7 اختبارات ناجحة.

## متطلبات النسخة الجديدة — فحص المجلد والمقارنة

تم اعتماد فحص كل الملفات داخل المجلد والمجلدات الفرعية افتراضيًا. تُصنف الملفات إلى `PE` و`Non-PE` و`Error` و`Skipped`. يعرض تقرير المجلد تفاصيل ملفات PE فقط، ويعرض غير PE كعداد مختصر، مع أقسام مستقلة للأخطاء والتجاوزات والملفات التي تحتاج مراجعة.

أضيف تجميع الملفات المتطابقة حسب SHA-256 داخل المجلد، كما أضيف أمر `compare` لمقارنة SHA-256 بين ملفين فقط. لا تعتبر نتيجة التطابق حكمًا على سلامة الملف.

## إضافة نظام التقييم الشامل

أضيفت وحدة `assessment.py` لتصنيف ملفات PE إلى `OK` أو `REVIEW_REQUIRED` مع أسباب قابلة للتفسير. يصبح الملف بحاجة إلى مراجعة عند اكتشاف packer معروف، أو Entropy مرتفعة جدًا، أو قسم قابل للتنفيذ والكتابة معًا، أو قسم مخصص عالي العشوائية.

لا يمثل التقييم حكمًا أمنيًا قطعيًا، بل يحدد الملفات التي تستحق اهتمام المحلل.

## اختبار وإصلاح أثناء التنفيذ

عند إضافة اختبار التقييم ظهر خطأ `NameError` بسبب عدم استيراد `build_result` داخل ملف الاختبارات. تم تسجيل الخطأ وإصلاحه بإضافة الاستيراد، ثم نجحت مجموعة الاختبارات كاملة.

أصبحت الاختبارات الحالية تحتوي على 10 اختبارات ناجحة، تشمل التقييم، فحص المجلد، مقارنة SHA-256، Entropy، HTML، إعدادات JSON، البصمات، والملفات غير الصالحة.

## توسيع قاعدة Packer Signatures

تمت إضافة مؤشرات محافظة لـ NsPack وPetite وPEBundle وPELock وRLPack وNeolite وUPack وWWPack وYoda Protector، مع تحسين مؤشرات PECompact وThemida/WinLicense وEnigma Protector. فُصلت العلامات النصية عن أسماء الأقسام، واستُخدمت أوزان منخفضة للمؤشرات المنفردة لتقليل النتائج الخاطئة.

تمت إضافة `signature_research.md` لتسجيل مصادر اختيار البصمات وحدود الاعتماد عليها. نجحت اختبارات قاعدة التوقيعات الموسعة وجميع اختبارات المشروع الـ11.

## تحسين التحقق البنيوي لملفات PE

أضيف تحقق صارم من حدود DOS Header وPE Header وFile Header وOptional Header وجدول الأقسام وبيانات الأقسام الخام. أصبحت الملفات المبتورة أو التي تشير أقسامها إلى بيانات خارج حدود الملف تُرفض برسالة واضحة بدل إنتاج تحليل غير موثوق.

أثناء الاختبار اكتُشفت عينة PE تجريبية غير صحيحة بنيويًا؛ تم إصلاح العينة بدل إضعاف التحقق. بعد الإصلاح نجحت جميع الاختبارات الـ11، بما فيها كشف UPX، Entropy، التقييم، فحص المجلد، المقارنة، والتقارير.

## تحسين معلومات المعمارية ونقطة الدخول

أضيف اسم معماري مقروء مثل `x86` و`x64` و`ARM64` بدل عرض قيمة `machine` الرقمية فقط. كما يحسب المحلل القسم الذي تقع فيه نقطة الدخول اعتمادًا على `EntryPoint RVA` ونطاقات الأقسام. أضيفت اختبارات تثبت أن العينة التجريبية تُعرّف كـ `x86` وأن نقطة دخولها تقع في `UPX0`، ونجحت جميع الاختبارات الـ11.

## تحليل Imports وExports

أضيف إلى محلل PE استخراج مكتبات الاستيراد والوظائف المستوردة من جدول Import Directory، مع دعم الاستيراد بالاسم أو بالـ ordinal، وإضافة قراءة Export Directory للوظائف المصدّرة وأرقامها وRVA الخاص بها. يعتمد التحليل على تحويل RVA إلى إزاحة داخل الملف مع التحقق من الحدود، ولا ينفّذ الملف محل التحليل. أضيفت الحقول إلى نتائج Text وJSON وHTML، وأضيف اختبار PE اصطناعي يحتوي `KERNEL32.dll!CreateFileA` وExport باسم `DllFunction`. أصبحت جميع الاختبارات الـ12 ناجحة.

## التحقق من PE32+ وCLI

أضيف اختبار مستقل لملف PE32+ بمعمارية x64 ويحتوي Import Thunk بعرض 64-bit، ونجح في قراءة `USER32.dll!WideCall`. أصبحت مجموعة الاختبارات تحتوي على 13 اختبارًا ناجحًا. كما تم تشغيل أوامر CLI فعليًا على عينة PE والتحقق من إخراج Text، وحفظ JSON، وإنشاء HTML، ثم تنظيف ملفات التجربة المؤقتة.
